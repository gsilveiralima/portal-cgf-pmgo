{
  "name": "portal-cgf-pmgo",
  "version": "2.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "audit": "node scripts/audit.mjs",
    "check": "npm test && npm run audit"
  },
  "dependencies": {
    "ai": "latest"
  },
  "engines": { "node": "22.x" }
}
